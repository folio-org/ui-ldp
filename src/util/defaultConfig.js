const defaultConfig = {
  // Managed in ../settings/RecordLimits.js
  defaultShow: 100,
  maxShow: 1000,
  maxExport: 10000,

  // Managed in ../settings/TableAvailability.js
  disabledTables: [],
};

export default defaultConfig;
